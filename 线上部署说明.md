# 线上访问

这个目录是纯静态网站，可以直接部署到 GitHub Pages、Cloudflare Pages、Netlify 或企业静态服务器。

## 本机与局域网访问

双击 `start-localhost.cmd` 会先把当前制作文件 `商用餐具AI视觉工作台.html` 同步为线上入口 `index.html`，再启动静态服务。窗口中会显示：

- 本机访问：`http://localhost:5500/index.html`
- 同一局域网其他电脑：`http://本机局域网IP:5500/index.html`

如果其他电脑无法访问，请在 Windows 防火墙中允许 TCP `5500` 入站，并确认两台电脑连接同一网络。`localhost` 只代表访问者自己的电脑，不能填写到其他电脑上。

如需重新安装开机启动快捷方式，请以 PowerShell 运行 `install-startup.ps1`；启动项会自动同步入口文件。

## 两台电脑共同更新

推荐使用 GitHub 仓库作为唯一主版本，使用 GitHub Pages 自动发布网页。两台电脑都安装 Git 和 Node.js，然后只编辑 `商用餐具AI视觉工作台.html`，不要直接编辑 `index.html`。

首次设置只需做一次：

1. 在 GitHub 新建一个空仓库，例如 `tableware-vision`。
2. 在本目录打开 PowerShell，执行：

```powershell
git init
git branch -M main
git remote add origin https://github.com/你的账号/tableware-vision.git
git add -A
git commit -m "Initial web app"
git push -u origin main
```

3. 在 GitHub 仓库的 Settings > Pages 中选择 `Deploy from a branch`、`main`、`/ (root)`。
4. 两台电脑都克隆同一个仓库，而不是各自复制一份文件：

```powershell
git clone https://github.com/你的账号/tableware-vision.git
```

日常更新流程：

- 开始修改前：`git pull`
- 修改 `商用餐具AI视觉工作台.html`
- 本机预览：双击 `start-localhost.cmd`
- 确认无误后：双击 `publish-web.cmd`
- 另一台电脑开始工作前：`git pull`

`publish-web.cmd` 会自动把制作文件同步到 `index.html`，提交网页文件并推送到 GitHub。GitHub Pages 完成发布后，所有电脑都通过同一个 `https://你的账号.github.io/tableware-vision/` 地址访问，不再使用 `localhost`。

注意：两台电脑不要同时修改同一段代码后直接推送；如果 Git 提示冲突，先保留需要的内容，再执行 `git add`、`git commit`、`git push`。API 密钥保存在浏览器本地，不会随 Git 上传。

部署前先运行 `start-localhost.cmd`，确认 `index.html` 已同步。部署时上传以下内容并保持目录结构不变：

- `index.html`
- `manifest.webmanifest`
- `sw.js`
- `icon.svg`
- `案例图/`

部署完成后，其他电脑使用托管平台提供的 `https://...` 地址访问即可。首次在线打开后，页面壳和案例图会进入离线缓存；AI 生图、对话和在线模型加载仍需要网络及对应 API。

GitHub Pages 最短流程：新建一个仓库，把以上文件上传到仓库根目录，在仓库的 Settings > Pages 中选择 `Deploy from a branch`、`main`、`/ (root)`，保存后使用 GitHub 生成的 Pages 地址。